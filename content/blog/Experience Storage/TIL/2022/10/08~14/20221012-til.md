---
title: 20221012-TIL
date: 2022-10-12 23:02:00
category: TIL
draft: false
tags:
  - "TIL"
---

오늘 알고리즘 문제는 아이디어만 잘 정리하면 빠르게 풀 수 있는 문제였다.

- 배 문제는 무게 제한이 큰 크레인, 무거운 박스부터 확인하는 식으로 풀었다.
- 처음에 조금 이상하게 구현해서 질문 게시판 반례에 막혔었다. (왜 그랬지?;)  
  `(크레인, 박스를 리스트에 저장 -> pop() 연산으로 하나씩 삭제하도록 구현했음)`
- collections.Counter() 를 써서 구현하려다가 시간 복잡도 때문에 막혔었다;  
  `(원소의 개수가 매번 줄어든다는 점을 놓쳐서, O(m^2 log m) 이라고 생각했음)`
- 시간 복잡도 문제로 막혀서 결국 검색해서 풀었다.. (너무 복잡하게 생각함;)  
  `(중첩 반복문 + list().remove() 을 써서 구현했는데 통과한 사람이 있었음)`
- 옮길 수 없는 박스의 개수를 활용하는 식으로 푼 사람도 있었다. (ㄹㅇ 천재)  
  `(현재 크레인이 못 옮기는 박스는 나머지 크레인들이 나눠 들어야 함을 이용 ㄷ;)`
