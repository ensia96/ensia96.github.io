import styled from 'styled-components'

export default styled.code`
  border-radius: 1em;
  border-style: solid;
  border-width: 2px;

  display: inline-block;

  line-height: normal;

  margin: 0.1em 0.3em;
  padding: 0.1em 0.6em;
`
